pipeline {
    agent any

    stages {
        stage('Build Image'){
            steps {
                script {

                   pipeline {
    agent any

    stages {
        stage('Build Image'){
            steps {
                script {

                  dockerapp = docker.build("updateinformatica/api-produto:1.0", '-f ./src/Dockerfile ./src')
                }
                
          }
        }
    }
}
                }
                
          }
        }
    }
}